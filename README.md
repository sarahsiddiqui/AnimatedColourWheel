# AnimatedColourWheel

The python library pygame was utilized to create 2D graphics of an animated moving square that has random colours. The function random.randint(0, 255) was used to simulate "animated" colours.
